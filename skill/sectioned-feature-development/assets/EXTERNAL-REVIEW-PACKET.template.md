# External Review Packet

- Review ID:
- Provider:
- Review kind:
- Base / head:
- Requirements:
- Scope manifest:
- Explicit exclusions:
- PLAN / contract:
- Existing findings (only for continuation):
- Required output schema:
- Report path:

For a PLAN challenge only: instruct the reviewer to read `references/planning/router.md` and the four short domain/language/adapter/concern catalogs first, then the actual matching guides; universal is only for a named uncovered part. Include the selected `references/planning/` paths/reasons and shared boundary examples, not the native reviewer's verdict or full research conversation. Reuse the same authority/non-goals and reject checklist-created scope. A code-only DELTA does not reload the planning library.
