interface:
  display_name: "Code Review"
  short_description: "Bounded review with delegated parent-owned acceptance"
  default_prompt: "Use $code-review for one bounded pass. In DELEGATED_PASS context return candidates and exact evidence; the parent owns admission and acceptance."
