interface:
  display_name: "Sectioned Feature Development"
  short_description: "Split large changes with auto re-decomposition"
  default_prompt: "Use $sectioned-feature-development to implement this large change as reviewable sections, require two clean reviews per section, and automatically re-split any section that hits the five-review hard cap."
