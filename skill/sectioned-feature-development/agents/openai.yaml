interface:
  display_name: "Sectioned Feature Development"
  short_description: "Bounded delivery, compact ZAS observe, paired audit"
  default_prompt: "Use $sectioned-feature-development with frozen implementation tiers and the installed ZAS observation contract; observe only on suspected loops and publish a paired ZAS audit without changing review assurance."
